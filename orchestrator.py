from openstack_builder import build_vns, build_vms, build_sec_groups, build_router

def openstack():
    # build_vns()
    # build_vms()
    # build_sec_groups()
    build_router()
if __name__ == "__main__":
    openstack()
